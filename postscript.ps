% postscript.ps – Basic PostScript example drawing shapes

% Set up font for text
/Times-Roman findfont 16 scalefont setfont

% 1) Draw Hello text
72 750 moveto (Hello, PostScript!) show

% 2) Define parameters
/count 6 def   % number of shapes
/radius 20 def % circle radius
/spacing 50 def

% 3) Draw row of circles
100 600 moveto
0 0 translate

0 1 count 1 sub {
  /i exch def
  newpath
  i spacing mul radius add 0 360 arc
  stroke
} for

% 4) Draw a filled rectangle
newpath
200 450 moveto
300 450 lineto
300 400 lineto
200 400 lineto
closepath
1 0 0 setrgbcolor fill

% 5) Define & call a procedure
/greet {
  % expects a string on stack
  /str exch def
  /Times-Roman findfont 12 scalefont setfont
  72 350 moveto str show
} def

(Hello from PS Procedure!) greet

showpage
